services:
  grafana:
    extends:
      file: .config/docker-compose-base.yaml
      service: grafana
    ports: !override
      - 3004:3000/tcp
    volumes:
      - grafana-storage:/var/lib/grafana

volumes:
  grafana-storage:
